{
  # TODO: Implement general mappings
  plugins.which-key = {
    enable = true;
    settings = {
      preset = "helix";
      icons = {
        ignoreMissing = false;
        breadcrumb = "»";
        group = "+";
        separator = ""; # ➜
      };
    };
  };
}
