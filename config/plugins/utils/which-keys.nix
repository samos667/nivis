{
  # TODO: Implement general mappings
  plugins.which-key = {
    enable = true;
    settings = {
      icons = {
        ignoreMissing = false;
        breadcrumb = "»";
        group = "+";
        separator = ""; # ➜
      };
    };
  };
}
